/// Material 3 elevation levels used consistently across shared components
/// (cards, dialogs, app bars) instead of ad hoc per-widget shadow values.
abstract class AppElevation {
  static const level0 = 0.0;
  static const level1 = 1.0;
  static const level2 = 3.0;
  static const level3 = 6.0;
  static const level4 = 8.0;
}
